protokoll 21.08.2017
cedric regenass


- java classen muessen in pakete strukturiert werden
- java classen muessen gleich heissen wie das file CASE-SENSITIVE
- fils muessen in dem verzeichnis liegen, damit der paket name uebereinstimmt
    - z.B packege ch.hf.math -> ordnerstruktur ch/hf/math/XYZ.java
- javac = java Compiler
- javap = java Package...
- javac -classpath "ch.hf.math" -> setzt einen temporaeren classenpfad fuer den compiler
- beim ausfuehren wird nicht ein filname ausgefuehrt sondern die classe (ohne .class)
- classpath gibt an, in welchen verzeichnissen gesucht wird, um zu compiliern oder auszufuehren
    - java -calsspath statistics:statistic2 ch.hf.math.StatisticsTest
- java archive = viele dateien in eine (.jar)
- jar cvf classes.jar ch
    - c = create v = verbose f = file
- jar tf zeigt alle files in dem jar file an
- java -cp classes.jar ch.hfict.math.StatisticsTest (aus archiv ausfuehren) [-cp = -classpath]
- jar cvfm classes.jar manifest.mf
    - c = create v = verbose f = file m = manifest datei // macht ausfuerbahres jar-file
- java -jar classes.jar // sobald manifest gemacht wurde
- COMMAND FOR SPLITTING SRC AND BIN -> javac -d bin -sourcepath src src/ch/hfict/math/StatisticsTest.java
-




- find . -name '*.class' -delete